


import numpy
import json
import sys

import dummy_model

if len(sys.argv) < 2:
	print('Usage: %s <config file (JSON)>'%sys.argv[0])
	sys.exit()

config_file_name = sys.argv[1]

#Get stuff from the JSON file


#Eventually...
#roads = dummy_model.generate_roads( size_x, size_y, data_layers, (starting_point_x, starting_point_y))

#Now analyse the result


